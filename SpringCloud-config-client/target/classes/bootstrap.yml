#系统级别的配置
spring:
  cloud:
    config:
      #http://localhost:8090/master/config-client-dev.yml
      name: config-client #需要从git上读取的资源名称，不需要后缀
      uri: http://localhost:8090
      profile: dev
      label: master
